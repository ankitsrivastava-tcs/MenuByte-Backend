package com.menubyte.enums;

public enum Visibility {
    ALL_USERS, // Visible on the showcase and applied in cart
    ADMIN_ONLY // Only applied if the code is manually entered/available (not shown in public lists)
}